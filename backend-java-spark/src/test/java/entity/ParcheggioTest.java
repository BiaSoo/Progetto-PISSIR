
package entity;

public class ParcheggioTest {

    /*private entity.Parcheggio parcheggio = new Parcheggio("A001", 1, false, 123, false);

    @BeforeEach
    void setUp() {
    }

    @Test
    void testGettersAndSetters() {
        assertEquals("A001", parcheggio.getId_parcheggio());
        assertEquals(1, parcheggio.getPosto());
        assertEquals(false, parcheggio.isOccupato());
        assertEquals(123, parcheggio.getId_veicolo());
        assertEquals(false, parcheggio.isFlag_is_entrato());

        parcheggio.setId_parcheggio("B002");
        parcheggio.setPosto(2);
        parcheggio.setOccupato(true);
        parcheggio.setId_veicolo(456);
        parcheggio.setFlag_is_entrato(true);

        assertEquals("B002", parcheggio.getId_parcheggio());
        assertEquals(2, parcheggio.getPosto());
        assertEquals(true, parcheggio.isOccupato());
        assertEquals(456, parcheggio.getId_veicolo());
        assertEquals(true, parcheggio.isFlag_is_entrato());
    }

    @Test
    void testGetData_ora_arrivo() {
        LocalDateTime now = LocalDateTime.now();
        parcheggio.setData_ora_arrivo(now);
        assertEquals(now, parcheggio.getData_ora_arrivo());
    }

    @Test
    void testSetData_ora_arrivo() {
        LocalDateTime dateTime = LocalDateTime.of(2024, 5, 20, 12, 0);
        parcheggio.setData_ora_arrivo(dateTime);
        assertEquals(dateTime, parcheggio.getData_ora_arrivo());
    }*/
}

